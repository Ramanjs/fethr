/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['www.pngitem.com','images.unsplash.com','plus.unsplash.com'],
      },
}

module.exports = nextConfig